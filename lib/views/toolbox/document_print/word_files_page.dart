import 'package:flutter/material.dart';

class WordFilesPage extends StatelessWidget {
  const WordFilesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [Text("Word Files")],),);
  }
}
