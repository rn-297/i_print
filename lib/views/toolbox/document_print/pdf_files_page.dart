import 'package:flutter/material.dart';

class PdfFilesPage extends StatelessWidget {
  const PdfFilesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [Text("Pdf Files")],),);
  }
}
