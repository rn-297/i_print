import 'package:flutter/material.dart';

class StickyNotePage extends StatelessWidget {
  const StickyNotePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Sticky note page")),
    );
  }
}
