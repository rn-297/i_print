import 'package:flutter/material.dart';

class StickyNoteEditPage extends StatelessWidget {
  const StickyNoteEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Sticky note edit page")),
    );
  }
}
