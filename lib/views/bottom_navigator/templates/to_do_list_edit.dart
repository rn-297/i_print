import 'package:flutter/material.dart';

class ToDoListEditPage extends StatelessWidget {
  const ToDoListEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("To Do List Edit Page"),
    );
  }
}
