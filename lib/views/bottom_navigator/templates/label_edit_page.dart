import 'package:flutter/material.dart';

class LabelEditPage extends StatelessWidget {
  const LabelEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Label Edit Page"),),);
  }
}
